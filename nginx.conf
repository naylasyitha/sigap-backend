server {
    listen 80;
    server_name paw-nayla.bccdev.id;

    location / {
        proxy_pass http://app:8085;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}